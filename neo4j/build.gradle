group 'com.mesosphere.sdk'
version '1.1-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'application'

repositories {
    mavenCentral()
    maven {
        url "http://downloads.mesosphere.com/maven/"
    }
    maven {
        url "http://downloads.mesosphere.com/maven-snapshot/"
    }
}

ext {
    junitVer = "4.12"
    systemRulesVer = "1.16.0"
    mockitoVer = "1.9.5"
}

dependencies {
    compile "mesosphere:scheduler:0.15.0"
    testCompile "mesosphere:testing:0.15.0"
}


distributions {
    main {
        baseName = 'neo4j-scheduler'
        version = ''
    }
}

mainClassName = 'com.mesosphere.sdk.neo4j.scheduler.Main'
